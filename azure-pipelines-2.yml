trigger:
- master
- develop

stages:

- stage: 'Build' 

  jobs:
  - job:
    pool:
      vmImage: 'windows-latest'   

    steps:
    - checkout: none
    - script: echo "$(Pipeline.Workspace)"
    - script: echo "$(Build.ArtifactStagingDirectory)"
    - script: echo "$(Build.BuildNumber)"
    - script: echo "$(Build.Build)"